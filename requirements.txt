flask>=3.0.0
flask-compress>=1.14
flask-limiter>=3.5
# Optional but handy: load PINAKES_* env vars from a local .env file when
# running `python app.py`. Production (Fly) uses `fly secrets set` and
# bypasses this entirely.
python-dotenv>=1.0
requests>=2.31.0
feedparser>=6.0.11
beautifulsoup4>=4.12.0
lxml>=5.0.0
apscheduler>=3.10.0
gunicorn>=21.2.0
networkx>=3.2
sentry-sdk[flask]>=2.0
# Backup pipeline (D2). pyrage produces standard age binary format so the
# operator can decrypt with the regular `age` CLI in a disaster.
boto3>=1.34
zstandard>=0.22
pyrage>=1.2
