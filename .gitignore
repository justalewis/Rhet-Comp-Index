# Database — never commit, lives on the Fly.io volume
articles.db
articles.db-shm
articles.db-wal
*.db
*.sqlite
*.sqlite3

# Datastories disk cache (derived; regenerated on demand from articles.db)
data/datastories_cache/

# Runtime-curated disciplinary events (admin-added via API; per-deployment)
data/disciplinary_events.json

# Python
__pycache__/
*.py[cod]
*$py.class
*.pyo
.Python
env/
venv/
.venv/
ENV/
pip-log.txt
pip-delete-this-directory.txt
.eggs/
*.egg-info/
dist/
build/

# Environment / secrets
.env
.env.*
*.env

# IDEs & editors
.vscode/
.idea/
*.swp
*.swo
*~

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Logs
*.log
logs/

# Test artifacts
.coverage
.coverage.*
htmlcov/
.pytest_cache/

# Misc
headers.txt

# Methodology overhaul scratch space (notes + raw example outputs)
_methodology_notes/
_methodology_examples/

# Substack drafts (local only, not part of the app) — both Word and Markdown
Change Documentation/Substack*

# Secrets / credentials — NEVER commit
Backblaze application key.docx
*application key*.docx
*.age.key

# Throwaway agent worktrees (nested git clones — would corrupt this repo)
.claude/worktrees/

# Office temp lock files
~$*

# Separate sibling projects that live under this folder but are not part of
# this app (each should be versioned in its own repo, not committed here).
canvas-sandbox/
google-forms/
